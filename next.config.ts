import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['animalfactguide.com', 'unsplash.com', 'aloks994.wpcomstaging.com'],
  }
};

export default nextConfig;
